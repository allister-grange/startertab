import React from "react";
import { AppErrorBoundary } from "@/components/ui/AppErrorBoundary";

const ServerErrorBoundary: React.FC = () => {
  return <AppErrorBoundary />;
};

export default ServerErrorBoundary;
